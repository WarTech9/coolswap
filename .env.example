# Solana RPC endpoint (optional - defaults to public mainnet)
# For production, use a dedicated RPC provider like Helius, QuickNode, or Triton
VITE_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Solana WebSocket endpoint (optional - auto-derived from RPC URL if not set)
# VITE_SOLANA_WS_URL=wss://api.mainnet-beta.solana.com

# deBridge API endpoints (optional - uses defaults)
VITE_DEBRIDGE_DLN_API_URL=https://dln.debridge.finance/v1.0
VITE_DEBRIDGE_STATS_API_URL=https://dln-api.debridge.finance/api

# Relay API configuration
VITE_RELAY_API_URL=https://api.relay.link
VITE_RELAY_API_KEY=your-relay-api-key-here

# Kora gas sponsorship service URL
VITE_KORA_URL=http://localhost:8080

# Server wallet public key (fee payer for Relay transactions)
# Generate using: node scripts/generate-server-wallet.js
VITE_SERVER_WALLET_PUBLIC_KEY=your-server-wallet-public-key-here

# Backend API URL (optional - leave empty for local dev with Vite proxy)
# Local dev: leave empty (Vite proxies /api/* to port 3001)
# Production: set to your Vercel deployment URL (e.g., https://coolswap.vercel.app)
VITE_BACKEND_URL=
